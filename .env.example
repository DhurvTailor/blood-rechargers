# Optional: enables the AI Assistant to use a real Claude model instead of the
# built-in rules engine fallback. Leave blank to run with zero configuration.
ANTHROPIC_API_KEY=

# Base URL used for metadata / OpenGraph tags
NEXT_PUBLIC_SITE_URL=https://www.bloodrechargers.com
